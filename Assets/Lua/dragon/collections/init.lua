local dict = require "dragon.collections.dict"
local queue = require "dragon.collections.queue"
local tree = require "dragon.collections.tree"

local M = {
    dict = dict,
    queue = queue,
    tree = tree
}

return M